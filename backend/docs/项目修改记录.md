# 项目修改记录

---

## 第 1 轮：基础安全检查

### 修改时间

2026-05-18

### 修改目标

修复后端代码中的功能正确性问题 — 包括答题计数逻辑 bug、ORM 关系缺失导致的 N+1 查询、文件上传无校验、批量导入事务缺陷四项高优先级问题。

### 修改内容

1. **修复 `quiz_service.submit_answer` 计数逻辑**
   - 文件：`app/services/quiz_service.py`
   - 问题：`session` 配置了 `autoflush=False`，先 `db.add(attempt)` 再执行 `count()` 查询时，新增 attempt 对 count 不可见，导致 `questions_done` 计数不准确
   - 修复：在 count 查询前显式调用 `db.flush()`；`sp.questions_done` 改为直接从 count 结果赋值，而非 `+= 1`

2. **`QuizAttempt` 模型添加 `question` 关系，消除 N+1 查询**
   - 文件：`app/models/entities.py`、`app/services/quiz_service.py`
   - `QuizAttempt` 新增 `question = relationship("Question")`
   - `get_quiz_history` 使用 `joinedload(QuizAttempt.question)` 预加载，替代原先循环内逐条查询 Question 的 N+1 模式

3. **文件上传增加安全校验**
   - 文件：`app/core/upload_validation.py`（新建）、`app/api/v1/routes/upload_routes.py`、`question_routes.py`、`class_routes.py`
   - 通用上传接口：限制允许的文件类型（图片/文档/视频/压缩包）和 50MB 上限
   - Excel 导入接口：限制 `.xlsx` / `.xls` 和 10MB 上限
   - 所有上传接口在 `file.read()` 之后、写磁盘之前做校验，早期拦截非法文件

4. **批量导入事务保护修复**
   - 文件：`app/services/class_service.py`
   - 问题：Excel 导入学生在循环中 catch `SQLAlchemyError` 后调用 `db.rollback()`，导致已成功处理的行也被回滚
   - 修复：每行用 `db.begin_nested()` 保存点包裹，单行失败仅回滚该行，其他行不受影响

### 验证情况

- 所有修改文件通过 Python 编译检查
- `upload_validation` 校验逻辑通过单元测试（正常文件、不支持类型、超限文件、空文件名）

### 遗留问题

无

---

## 第 2 轮：需求文档未实现的功能

### 修改时间

2026-05-19

### 修改目标

对照 `docs/backend-features/` 中的需求文档，实现两个完全未落地的功能模块 — 课程时间安排（06 号需求）和作品批量下载 ZIP（05 号需求）。

### 修改内容

1. **课程时间安排（06-class-schedule.md）**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`、`app/api/v1/routes/chapter_routes.py`、`app/services/chapter_service.py`、`seed_data.py`
   - Chapter 模型新增 `day_of_week`、`class_periods`、`schedule_note` 三个字段
   - 新增 `ChapterScheduleUpdate` Pydantic Schema
   - `ChapterOut` 补充 `course_id`、`day_of_week`、`class_periods`、`schedule_note` 四个字段
   - 新增 `PUT /api/chapters/{id}/schedule` 端点（teacher 权限）
   - `chapter_service.list_chapters()` 和 `chapter_routes.get_chapter_detail()` 全部返回新字段
   - `seed_data.py` 前三章（01/02/03）补充排课时间示例

2. **作品批量下载 ZIP（05-project-upload-enhance.md）**
   - 文件：`app/api/v1/routes/teacher_routes.py`
   - 新增 `GET /api/teacher/projects/batch-download` 端点（teacher 权限）
   - 查询所有 `status='approved'` 且 `report_url` 非空的 Project，从 `uploads/` 读取 PDF 文件
   - 用 `zipfile` 打包为 ZIP（内存中），通过 `StreamingResponse` 返回
   - 文件名：`作品报告_{日期}.zip`，ZIP 内文件：`{作者名}_{标题}.pdf`

### 验证情况

- `Chapter` 模型新字段属性断言通过
- `ChapterScheduleUpdate` Schema 序列化/反序列化通过
- 所有模块成功导入，无语法错误

### 遗留问题

无

---

## 第 3 轮：代码质量与一致性修复

### 修改时间

2026-05-19

### 修改目标

补全 ORM 关系映射、清理仓库无用文件和 SQLite 残留、将路由中裸 `dict` 参数统一改为 Pydantic Schema。

### 修改内容

1. **补全实体模型 relationship**
   - 文件：`app/models/entities.py`、`app/services/announcement_service.py`、`app/services/task_service.py`
   - `Announcement` 新增 `class_`、`teacher`、`reads`、`completions` 四个关系
   - `AnnouncementRead` 新增 `announcement`、`user` 关系
   - `TaskCompletion` 新增 `announcement`、`user` 关系
   - `announcement_service._announcement_payload` 去掉手动 `db.query(Class)` / `db.query(User)` 查询，改用 `ann.class_` / `ann.teacher` / `ann.reads`
   - `task_service.completion_report` 去掉手动 `db.query(Class)`，改用 `ann.class_`

2. **无用文件清理 + .gitignore 更新**
   - 删除 `app/models/announcement_entities_patch.py`（内容仅 "placeholder"）
   - 删除 `tongshi.db-shm`、`tongshi.db-wal` SQLite WAL 残留文件
   - `.gitignore` 新增 `*.db`、`*.db-shm`、`*.db-wal`、`.claude/settings.local.json`

3. **路由 dict 参数 → Pydantic Schema**
   - 文件：`app/schemas/common.py`、`app/api/v1/routes/announcement_routes.py`、`question_routes.py`、`class_routes.py`
   - 新增 `AnnouncementCreate`、`CourseCreateRequest`、`CourseUpdateRequest`、`ClassEnrollRequest` 四个 Schema
   - `announcement_routes.create(data: dict)` → `create(data: AnnouncementCreate)`
   - `question_routes.add_course(payload: dict)` / `edit_course(payload: dict)` → 改用 `CourseCreateRequest` / `CourseUpdateRequest`
   - `class_routes.add_student(student_id: str)` query 参数 → `ClassEnrollRequest` body

### 验证情况

- 所有新 Schema 构造和序列化通过
- 实体关系属性存在性断言通过
- 删除的文件确认不再存在于磁盘和 git 跟踪

### 遗留问题

无

---

## 第 4 轮：种子数据完善 + 密码安全 + 框架升级

### 修改时间

2026-05-19

### 修改目标

完善开发用种子数据（班级/课程/公告）、增强注册密码复杂度校验、修复 FastAPI `on_event` 弃用警告。

### 修改内容

1. **种子数据完善**
   - 文件：`seed_data.py`
   - 新增 `Course`：创建 "AI 通识课"，6 个章节全部关联 `course_id`
   - 新增 `Class` + `StudentClassEnrollment`：创建 "2025级1班"（自动化）和 "2025级2班"（机械工程），5 名学生分配到两个班级
   - 新增 `Announcement`：2 条示例（课程通知 + 章节练习任务，含起止时间）

2. **密码复杂度增强**
   - 文件：`app/schemas/common.py`
   - `RegisterRequest.password` 新增 `@field_validator`：密码必须包含至少一个字母 + 至少一个数字，保留原有 `min_length=6`

3. **FastAPI 框架升级**
   - 文件：`main.py`
   - `@app.on_event("startup")` → `asynccontextmanager lifespan` 模式，消除启动时的 DeprecationWarning

### 验证情况

- 密码校验器：纯数字 "123456" 拒绝、纯字母 "abcdef" 拒绝、混合 "abc123" 通过
- `seed_data` 模块成功导入
- `main.py` 语法正确

### 遗留问题

无

---

## 第 5 轮：测试体系 + API 文档 + Alembic 迁移 + 操作日志

### 修改时间

2026-05-19

### 修改目标

建立集成测试体系、为所有端点添加 Swagger 文档注释、引入 Alembic 数据库迁移机制、在关键服务操作中添加操作日志。

### 修改内容

1. **API 集成测试**
   - 文件：`tests/conftest.py`（新建）、`tests/test_auth.py`（新建）
   - 使用 `pytest` + `httpx`，SQLite 内存数据库（`sqlite:///:memory:`），每次测试独立建库
   - 14 个测试用例覆盖：登录成功/密码错误/不存在用户、密码校验拒绝、重复注册、获取当前用户、章节列表/详情/不存在/权限拒绝、答题提交（正确+错误）/统计/历史
   - `conftest.py` 提供 `client`、`student_token`、`teacher_token` 等 fixture

2. **Swagger 文档注释**
   - 文件：全部 10 个路由文件
   - 每个端点添加 `summary`（中文短描述）和 `description`（中文详细说明），覆盖认证、章节、资料、题库、测验、作品、教师、班级、公告、成长档案、上传所有模块

3. **Alembic 数据库迁移**
   - 文件：`alembic.ini`、`migrations/env.py`、`migrations/script.py.mako`（新建）；`requirements.txt`
   - 安装并初始化 Alembic，`migrations/env.py` 自动读取 `app.core.config.settings.database_url` 作为迁移目标数据库
   - 后续修改模型后执行 `alembic revision --autogenerate -m "说明"` + `alembic upgrade head`

4. **服务层操作日志**
   - 文件：`app/services/auth_service.py`、`announcement_service.py`、`class_service.py`、`project_service.py`
   - 登录成功/失败 → `logger.info/warning` 记录 user_id 和 role
   - 注册成功 → 记录 user_id 和 role
   - 公告/班级创建和删除 → 记录操作者 ID 和对象信息
   - 作品提交/审核通过/驳回 → 记录 user_id、title、reason

### 验证情况

- `python -m pytest tests/ -v` → **14/14 passed**（耗时 1.3s）
- 所有模块通过编译检查

### 遗留问题

- 测试覆盖仅限核心模块（认证 + 章节 + 答题），公告、班级、作品等模块的测试可以后续补充
- Alembic 初始迁移需在 MySQL 可用后执行 `alembic revision --autogenerate -m "init"`

---

## 第 6 轮：课程体系管理联动

### 修改时间

2026-05-21

### 修改目标

将"课程"从独立名称管理完善为课程、章节、资料、课表和学生端学习入口的一级组织体系。

### 修改内容

1. **后端课程与章节接口完善**
   - 文件：`app/api/v1/routes/chapter_routes.py`、`app/api/v1/routes/question_routes.py`、`app/services/chapter_service.py`、`app/services/question_service.py`、`app/schemas/common.py`
   - 章节列表新增 `course_name` 返回字段
   - 新增教师端章节创建、完整编辑、删除接口
   - 章节排课接口支持同步修改 `course_id`
   - 课程列表返回 `chapter_count` 和 `material_count`
   - 新增课程详情接口，返回课程基本信息、统计和该课程下的章节列表
   - 删除课程时，如果课程下仍有章节，返回中文业务错误，避免误删课程结构
   - 删除章节时，如果章节下仍有资料、题目或学习记录，返回中文业务错误，避免误删教学内容

2. **教师端资料管理页联动课程和章节**
   - 文件：`frontend/src/views/teacher/TeacherMaterials.vue`
   - "管理课程"弹窗显示课程名称、章节数、资料数和创建时间
   - 课程详情抽屉展示课程下的章节
   - 支持新增、编辑、删除章节
   - "课程时间安排"按课程分组展示章节
   - 设置课表时可修改章节所属课程、星期、节次和备注

3. **学生端课程列表和课程详情页**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/CourseDetailView.vue`、`frontend/src/router/index.ts`
   - `/learn` 改为课程列表，显示每门课程的章节数和资料数
   - 新增 `/learn/course/:courseId` 课程详情页，展示该课程下章节、资料数量、排课和学习入口
   - 原章节学习页 `/learn/:chapterId` 保留，继续承载视频和 PDF 学习

4. **旧数据库字段兼容**
   - 文件：`app/db/schema_compat.py`、`main.py`、`database_setup.py`
   - 后端启动和数据库初始化时自动补齐旧 `chapters` 表缺失的排课字段：`day_of_week`、`class_periods`、`schedule_note`

### 验证情况

- `py -m pytest backend\tests -q` → **23 passed**，仅保留既有 Pydantic V2 `Config` 弃用警告
- `cd frontend && npm run build` → **通过**，仅保留 Vite chunk 体积提示

### 遗留问题

- 尚未用浏览器手动验证 `/teacher/materials`、`/learn`、`/learn/course/:courseId` 的实际交互路径
- 部分旧 Markdown 文档在终端中显示为乱码，未在本轮大规模重写

---

## 第 7 轮：教师数据隔离权限设计

### 修改时间

2026-05-22

### 修改目标

设计教师端数据隔离方案，确保不同教师之间的班级、课程、章节、资料、题目互不串扰。本轮产出设计文档和实施计划，尚未全面落地。

### 修改内容

1. **设计文档和实施计划**
   - 新建：`docs/superpowers/specs/2026-05-22-teacher-data-isolation-design.md`
   - 新建：`docs/superpowers/plans/2026-05-22-teacher-data-isolation-plan.md`
   - 设计文档覆盖：数据模型（5 张表加 `teacher_id`）、接口隔离、旧数据兼容策略
   - 实施计划分 5 个 Task（TDD 步骤），涉及 18 个文件

2. **前端错误提示优化**
   - 文件：`frontend/src/api/http.ts`
   - 新增 `getErrorMessage` 和 `normalizeValidationMessage` 函数
   - 后端 Pydantic 校验错误（如密码长度、必填字段）自动转为中文提示
   - 422 错误统一显示"提交内容不符合要求，请检查填写内容"

3. **注册页面微调**
   - 文件：`frontend/src/views/RegisterView.vue`
   - 适配新的错误提示机制

### 验证情况

- 设计文档和实施计划已提交，待后续按 Task 逐步实施

### 遗留问题

- 教师数据隔离的后端实施（Task 1-5）尚未启动，需要按计划 TDD 步骤逐步推进
- 后续实施完成后需回填本轮修改记录

---

## 第 8 轮：作品审核的改进

### 修改时间

2026-05-23

### 修改目标

完善作品管理功能：支持多图片上传、改进审核流程、优化作品展示和提交体验。

### 修改内容

1. **新增作品图片模型**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`
   - 新增 `ProjectImage` 模型，表 `project_images`（id、project_id、image_url、sort_order）
   - `Project` 新增 `images` 关系，级联删除
   - 新增 `ProjectImageOut` Schema，`ProjectOut` 增加 `images` 字段
   - 新增 `ProjectUpdate` Schema，支持 `image_urls` 列表

2. **作品服务功能增强**
   - 文件：`app/services/project_service.py`
   - 支持多图片创建和更新（`image_urls` 列表）
   - 改进审核逻辑和作品数据聚合

3. **数据库兼容扩展**
   - 文件：`app/db/schema_compat.py`、`tests/test_schema_compat.py`
   - 自动创建 `project_images` 表（旧库兼容）
   - 新增兼容性测试

4. **前端作品体验优化**
   - 文件：`frontend/src/views/ActView.vue`、`ProjectDetailView.vue`、`ProjectUploadView.vue`
   - 活动页面大幅重构（612 行变动）
   - 作品详情页支持图片展示
   - 作品上传页支持多图片

5. **教师端改进**
   - 文件：`frontend/src/views/teacher/TeacherReviews.vue`、`TeacherQuestions.vue`
   - 作品审核页面增加详情展示
   - 题库管理页面交互优化

### 验证情况

- `py -m pytest backend\tests -q` → 通过
- `cd frontend && npm run build` → 通过

### 遗留问题

- 尚未用浏览器手动验证多图片上传和作品详情展示的实际交互路径

---

## 第 9 轮：多存储改造（数据库重建）

### 修改时间

2026-05-25

### 修改目标

引入 SeaweedFS (S3) 作为新文件默认存储，建立统一文件元数据（StoredFile）与存储抽象层，并兼容历史 `backend/uploads` 文件与现有前端接口。依据 `docs/superpowers/plans/数据库重建.md` 的 Task 1~7 全部实施。

### 修改内容

1. **存储配置与依赖**
   - 文件：`app/core/config.py`、`requirements.txt`、`backend/README.md`
   - 新增 9 个存储配置字段（`storage_backend`、`s3_endpoint`、`s3_access_key` 等）
   - 新增依赖：`boto3`、`python-magic`

2. **文件元数据模型 StoredFile**
   - 文件：`app/models/entities.py`、`app/schemas/common.py`、`app/db/schema_compat.py`
   - 新增 `StoredFile` 模型（15 列：id、biz_type、storage_provider、object_key、content_type、sha256 等）
   - `Material` 新增 `file_id`，`Project` 新增 `report_file_id`/`cover_file_id`，`ProjectImage` 新增 `file_id`
   - Schema 兼容：`UploadOut` 增加 `file_id`/`content_type`/`storage_provider`，业务 Schema 增加 `file_id`
   - `schema_compat.py` 自动创建 `stored_files` 表 + 补齐新列

3. **存储抽象层**
   - 新建：`app/services/storage_service.py`（StoredObject dataclass + StorageAdapter Protocol）
   - 新建：`app/services/storage_local.py`（LocalStorageAdapter，读写本地 uploads 目录）
   - 新建：`app/services/storage_s3.py`（S3StorageAdapter，boto3 实现）
   - 新建：`app/services/file_service.py`（文件元数据写入、URL 构建、记录解析）

4. **统一文件访问路由**
   - 新建：`app/api/v1/routes/file_routes.py`（`GET /files/{file_id}` 返回 FileResponse/StreamingResponse）
   - 在 `app/api/v1/__init__.py` 注册 file_router
   - `main.py` 保留 `/uploads` 静态挂载，兼容历史 URL

5. **上传安全升级**
   - 文件：`app/core/upload_validation.py`、`app/api/v1/routes/upload_routes.py`
   - 新增 `detect_content_type()` 魔数检测（PDF/ZIP/PNG/JPEG/GIF/WebP）
   - 上传路由改造：读 content → 魔数检测 → 存储适配器保存 → 创建 StoredFile → 返回 file_id

6. **资料/作品/批量下载接线**
   - 文件：`app/services/material_service.py`、`material_routes.py`、`project_service.py`、`project_routes.py`、`teacher_routes.py`
   - 资料创建支持 `file_id`，路由返回 `file_id`
   - 作品创建/更新支持 `report_file_id`/`cover_file_id`/`image_file_ids`
   - 教师批量下载改为临时 ZIP 文件 + FileResponse（避免内存常驻）

7. **前端兼容**
   - 文件：`frontend/src/api/upload.ts`、`material.ts`、`project.ts`
   - `UploadResult` 增加 `file_id`/`content_type`/`storage_provider`
   - `Material`/`Project` 类型增加 `file_id` 相关字段

8. **测试覆盖**
   - 文件：`tests/test_schema_compat.py`（+3 测试）、`tests/test_integration_bugfixes.py`（+7 测试）
   - 新增 10 个测试：配置默认值、StoredFile 表创建、文件路由、上传返回 file_id、魔数校验、资料/作品 file_id 持久化、批量下载

9. **文档与忽略规则**
   - `.gitignore` 新增 `*.sqlite`、`*.sqlite3`、`backend/tmp/`、`backend/exports/`、`*.zip.tmp`
   - `backend/README.md` 更新目录结构、存储配置说明
   - `CLAUDE.md` 更新表数（16→17）、路由数（11→12）、存储说明

### 验证情况

- `py -m pytest backend\tests -q` → **39 passed**（含 10 个新增存储测试）
- `cd frontend && npm run build` → 通过
- Windows 环境修复：`fcntl` 模块条件导入、SQLAlchemy inspector 缓存刷新、PDF 魔数偏移修复

### 遗留问题

- 未验证真实 SeaweedFS S3 连接（本地使用 local 存储后端）
- 未验证 S3 权限错误分支、千兆级大包下载性能

---

## 第 10 轮：P1 — 批量下载 S3 适配改造

### 修改时间

2026-05-26

### 修改目标

修复批量下载在 S3 存储模式下完全不可用的问题。原实现硬编码了本地 `uploads` 路径且仅支持 `report_url`（旧字段），改为通过 `file_service.resolve_file_stream()` 统一获取文件流，自动适配 local/s3 两种存储后端，同时实现流式 ZIP 输出控制内存峰值。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P1。

### 修改内容

1. **文件定位逻辑统一（P1.1）**
   - 文件：`app/api/v1/routes/teacher_routes.py`
   - 移除硬编码路径 `Path(__file__).resolve().parents[4] / "uploads"`
   - 新增 `_get_project_file_content()` 函数，通过 `resolve_file_stream()` 统一获取文件流
   - S3 文件和历史本地文件使用同一入口，自动分发

2. **新旧字段兼容（P1.2）**
   - 优先通过 `report_file_id` → `stored_files` 表 → 存储适配器获取文件
   - 降级到 `report_url` → `settings.local_upload_dir` 读取历史本地文件
   - 查询条件从仅 `report_url` 扩展为两者 OR 条件

3. **流式 ZIP 打包（P1.3）**
   - ZIP 写入临时磁盘文件（避免所有文件同时驻留内存）
   - 通过 `_chunk_reader()` 生成器 64KB 分块 yield 给 `StreamingResponse`
   - 使用 `BackgroundTasks` 在响应发送完成后自动清理临时文件

4. **文件名安全增强（P1.4）**
   - 新增 `_sanitize_filename()` 函数，过滤控制字符（ASCII <32、DEL 127）、路径分隔符和通配符
   - 空文件名降级为 "unnamed"

### 验证情况

- `python -m pytest tests/ -v` → **39/39 passed**，无回归
- 两个原有批量下载测试（`test_batch_download_uses_standard_auth_header`、`test_batch_download_returns_zip_for_approved_projects`）均通过
- 历史作品（仅 `report_url`）和新作品（含 `report_file_id`）均可正常下载

### 遗留问题

- 本次仅修复了批量下载的文件定位和流式输出问题；魔数校验漏洞在后续 P3 中处理

---

## 第 11 轮：P2 — S3 存储适配器修复

### 修改时间

2026-05-27

### 修改目标

加固 S3 存储适配器的健壮性：添加 boto3 重试策略应对临时网络故障、`open_stream` 捕获 404 错误避免未处理异常导致 500、`resolve_file_stream` 加强异常兜底。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P2。

注：P2.1（`s3_force_path_style` 传递）已在上游 PR #2 合并时修复，本轮仅补做 P2.2 和 P2.3。

### 修改内容

1. **添加 boto3 重试策略（P2.2）**
   - 文件：`app/services/storage_s3.py`
   - `S3StorageAdapter.__init__` 始终配置 `retries={"max_attempts": 3, "mode": "standard"}`
   - 重构 `Config` 构建逻辑：使用 `config_kwargs` 字典统一管理 `retries` 和条件 `s3.addressing_style`

2. **open_stream 404 错误处理（P2.3）**
   - 文件：`app/services/storage_s3.py`
   - `open_stream` 捕获 `botocore.exceptions.ClientError`，识别 `NoSuchKey`/`404` 错误码
   - 转换为 Python 标准 `FileNotFoundError`（不带 Web 框架依赖），携带 bucket/key 上下文
   - 其他 `ClientError` 继续向上抛出

3. **resolve_file_stream 异常兜底**
   - 文件：`app/services/file_service.py`
   - `resolve_file_stream` 对 `open_stream` 调用增加 try/except
   - 捕获 `FileNotFoundError` 和其他异常，统一返回 `(record, None)`
   - 防止 `exists()` 和 `open_stream()` 之间的竞态条件导致 500

### 验证情况

- `python -m pytest tests/ -v` → **40/41 passed**（1 个预存失败：`test_settings_reads_seaweedfs_s3_config` 因 `load_dotenv(override=True)` 覆盖 monkeypatch 导致，与本次改动无关）
- C 组相关全部测试（上传、文件访问、批量下载、存储 schema 兼容）均通过

### 遗留问题

- S3 适配器仍无基于 moto mock 的单元测试（计划在 P4 中补齐）
- `save_bytes` 和 `delete` 未添加重试/异常处理（当前影响较小，可后续统一加固）

---

## 第 11 轮：超级大更改（管理员系统 + 个人中心 + 悟页面 + 课程管理 + 登录安全增强）

### 修改时间

2026-05-27

### 修改目标

一次性落地杨力提出的功能需求：管理员教师账号管理系统、学生个人中心（错题本+收藏）、悟页面图文内容管理、教师独立课程管理页面，以及首次登录强制改密等安全增强。共涉及 45 个文件，4393 行新增。

### 修改内容

1. **管理员系统（admin 角色）**
   - 后端：新建 `admin_routes.py`（`/api/admin/teachers`），支持教师账号 CRUD、Excel 批量导入、密码重置
   - `security.py`：`get_current_user` 返回增加 `needs_password_change` 字段；支持 `require_role("admin")`
   - `entities.py`：`User` 模型新增 `needs_password_change = Column(Boolean, default=False)`
   - `schemas/common.py`：新增 `CreateTeacherRequest`、`TeacherInfo`、`ChangePasswordRequest`、`ForgotPasswordRequest`
   - `auth_service.py`：新增 `forgot_password()` 函数
   - 前端：新建 `admin.ts`（API 封装）、`AdminLayout.vue`、`AdminTeachers.vue`、`AdminShowcase.vue`
   - 种子数据：`seed_data.py` 新增管理员账号

2. **学生个人中心**
   - 后端：新建 `profile_routes.py`（`/api/profile`），提供错题本 + 收藏作品两个接口
   - `quiz_service.py`：新增 `get_wrong_questions()`，取每道题最近一次答错记录
   - `project_service.py`：新增 `list_liked_projects()`，返回用户点赞过的所有作品
   - 前端：新建 `profile.ts`（API 封装）、`ProfileView.vue`（错题本 + 收藏作品 Tab 页）、`ChangePasswordView.vue`

3. **悟页面图文内容管理（Showcase）**
   - `entities.py`：新增 `ShowcaseItem` 模型，表 `showcase_items`（id、section、title、content、cover_file_id、link_url、sort_order、is_active、created_by、created_at、updated_at）
   - `schemas/common.py`：新增 `ShowcaseItemCreate`、`ShowcaseItemUpdate`、`ShowcaseItemOut`
   - 后端：新建 `showcase_routes.py`（`/api/showcase`），管理员增删改查 + 公开只读，按 section（welfare/reading_club）分组
   - 前端：新建 `showcase.ts`（API 封装）、`AdminShowcase.vue`

4. **教师课程管理页面**
   - 前端：新建 `TeacherCourses.vue`，独立的课程管理视图
   - `TeacherLayout.vue`：导航菜单新增"课程管理"入口

5. **登录安全增强**
   - `needs_password_change` 字段：新建教师账号时标记为 `True`，首次登录必须改密
   - `router/index.ts`：新增 `beforeEach` 守卫，已登录且 `needs_password_change=true` 时强制跳转 `/change-password`
   - 管理员访问首页自动跳转 `/admin` 后台
   - 管理员路由新增 `role: 'admin'` 鉴权守卫
   - `auth.ts`（前端）：`changePassword()` 和 `forgotPassword()` API

6. **其他前端增强**
   - 平台名称从"探 · 练 · 创 · 行"改为"学 · 思 · 践 · 悟"
   - `ActView.vue`：大幅重构（394+ 行变更）
   - `TeacherAnnouncements.vue`：公告管理功能增强（387+ 行变更）
   - `TeacherQuestions.vue`：题库管理增强（263+ 行变更），`QuestionOut` Schema 增加 `chapter_name`、`course_id`、`course_name`
   - `LoginView.vue`：登录页增强（177+ 行变更），支持忘记密码
   - `ClassEnrollRequest`：新增 `name` 字段，注册时可同时指定姓名
   - 首页组件调整：`AppHeader.vue`、`AppFooter.vue`、`HeroSection.vue`、`ModuleShowcase.vue` 等

7. **数据库兼容**
   - `schema_compat.py`：新增自动补齐 `users.needs_password_change` 列
   - `schema_compat.py`：新增自动创建 `showcase_items` 表（需在 `ensure_schema_compatibility` 中添加）

8. **路由注册**
   - `__init__.py`：新增 `admin_router`（prefix="/admin"）、`profile_router`、`showcase_router` 三个路由注册

### 验证情况

- 前后端代码语法正确
- **缺少实际测试**：本次变更涉及 45 个文件但未补充对应的集成测试

### 遗留问题

- 管理员 CRUD、个人中心错题本/收藏、悟页面 CRUD、密码修改/重置流程均无测试覆盖
- `showcase_items` 表在 `schema_compat.py` 中的自动创建逻辑可能需要补充（当前 diff 中未见到）
- `QuestionOut` Schema 新增字段需要确认前端 TeacherQuestions.vue 的展示逻辑是否完整
- 部分旧 Markdown 文档（如 CLAUDE.md 中的路由/表计数）需同步更新

---

## 第 12 轮：S3 文件预览与 MySQL 运行配置修复

### 修改时间

2026-05-28

### 修改目标

保留 SeaweedFS/S3 中已有文件，不迁回本地；业务数据继续使用 MySQL。修复学生端章节资料中视频和 PDF 无法在浏览器正常查看的问题。

### 修改内容

1. **统一文件访问支持浏览器 Range 请求**
   - 文件：`app/api/v1/routes/file_routes.py`
   - `GET /api/files/{file_id}` 新增 `Range` 请求解析
   - 对合法范围返回 `206 Partial Content`
   - 响应头包含 `Accept-Ranges`、`Content-Range`、`Content-Length`
   - 解决浏览器 `<video>` 播放和 PDF 预览需要分段读取的问题

2. **保持 MySQL + S3 运行配置**
   - 文件：`backend/.env`
   - `DATABASE_URL` 使用 `mysql+pymysql://.../tongshi`
   - `STORAGE_BACKEND=s3`
   - SeaweedFS S3 网关继续使用 `S3_ENDPOINT=http://localhost:8333`

3. **教师端资料上传保存 file_id**
   - 文件：`frontend/src/views/teacher/TeacherMaterials.vue`
   - 创建资料时提交上传接口返回的 `file_id`
   - `materials.file_id` 可稳定指向 `stored_files.id`
   - 前端仍通过 `/api/files/{file_id}` 访问文件，不直接暴露 SeaweedFS 对象地址

4. **回归测试补充**
   - 文件：`backend/tests/test_integration_bugfixes.py`
   - 新增 `/api/files/{file_id}` Range 分段访问测试

### 验证情况

- `cmd /c backend\start_backend.bat -CheckOnly` 通过：MySQL 可连接，SeaweedFS S3 网关可达，后端健康检查可用
- `curl` 请求现有视频 `/api/files/2` 返回 `206 Partial Content`，`content-type: video/mp4`
- `curl` 请求现有 PDF `/api/files/1` 返回 `%PDF-1.5`，`content-type: application/pdf`
- `python -m pytest backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_get_file_by_file_id_returns_streamed_content backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_get_file_by_file_id_supports_range_requests backend/tests/test_integration_bugfixes.py::TestIntegrationBugfixes::test_create_material_persists_file_id -q` 通过（3 passed）
- `cmd /c npm run build` 通过

### 遗留问题

- 当前验证使用真实本地 SeaweedFS 网关和已有文件记录，尚未补充 S3 mock 单元测试
- PowerShell 直接执行 `npm run build` 会被本机执行策略拦截，Windows 下可用 `cmd /c npm run build`
- 当前 shell 中 `git` 不可用，无法通过 `git status` 核对工作区状态

---

## 第 13 轮：上传安全加固 + 前端体验优化（P3 + P5）

### 修改时间

2026-05-30

### 修改目标

堵住文件上传魔数校验漏洞（MP4/MOV/WebP）、补齐 DOCX/PPTX 容器格式检查和 SVG 安全清洗；同时优化前端上传和审核体验（biz_type 分类、图片缩略图、搜索筛选、文案统一）。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P3 + P5。

### 修改内容

**P3 — 上传安全校验加固**

1. **MP4/MOV 魔数修复**
   - 文件：`app/core/upload_validation.py`
   - 问题：旧校验 `[b"\x00\x00\x00", b"ftyp"]` 对任意以三个 null 字节开头的文件放行，ftyp 未限定偏移
   - 修复：`validate_magic_number` 对 `.mp4`/`.mov` 校验偏移 4 字节处必须为 `ftyp`（ISOBMFF 标准）

2. **WebP 双重校验**
   - 文件：`app/core/upload_validation.py`
   - 修复：校验偏移 0 处为 `RIFF` **且** 偏移 8 处为 `WEBP`，防止 AVI/WAV 误过

3. **DOCX/PPTX ZIP 容器 PK 头检查**
   - 文件：`app/core/upload_validation.py`
   - 修复：将 `.docx`/`.pptx` 从 `_SKIP_MAGIC_EXTENSIONS` 移除，添加 `PK` 头校验
   - `detect_content_type` 新增 docx/pptx MIME 类型推断
   - `.svg`（XML 文本格式）和 `.doc`（旧格式魔数复杂）保留跳过

4. **SVG 安全清洗器**
   - 新建：`app/core/svg_sanitizer.py`
   - 移除危险标签（`<script>`、`<foreignObject>`、`<use>`）
   - 移除 `on*` 事件属性（`onclick`、`onload` 等）
   - 移除 `<!DOCTYPE` 声明和 `<!ENTITY` 外部实体引用
   - 集成到 `upload_routes.py`：SVG 上传时先清洗再存储，清洗后为空则拒绝

**P5 — 前端体验优化**

5. **上传增加 biz_type 分类**
   - 文件：`upload_routes.py` + `upload.ts` + `ProjectUploadView.vue`
   - 后端接收 `biz_type` 表单字段（默认 `"upload"`），写入 `stored_files.biz_type`
   - 前端：报告上传传 `"project_report"`，图片上传传 `"project_image"`
   - 同时补齐 `report_file_id` 和 `image_file_ids` 的提交

6. **编辑作品时显示图片缩略图**
   - 文件：`ProjectUploadView.vue`
   - 已有图片 URL 渲染为 32×32 缩略图（`<img>` 标签），可直观辨认已上传图片

7. **审核列表搜索 + rejected 筛选**
   - 后端：`teacher_service.py` `list_all_projects` 新增 `keyword` 参数（`title LIKE`）
   - 后端：`teacher_routes.py` `GET /teacher/projects` 新增 `keyword` 查询参数
   - 后端：去掉默认 `status.in_(["pending", "approved"])` 硬过滤，使 `rejected` 筛选生效
   - 前端：`TeacherReviews.vue` 新增搜索框（支持回车搜索 + 清除重置）
   - 前端：状态筛选下拉补全 `rejected`（驳回）选项
   - 前端：`teacher.ts` `getAllProjects` 新增 `keyword` 参数

8. **文件大小限制统一 50MB**
   - 文件：`upload_validation.py` + `ProjectUploadView.vue`
   - 后端 `MAX_UPLOAD_SIZE` 从 2GB 改为 50MB
   - 前端上传提示文案从"限 20MB"改为"限 50MB"

### 验证情况

- `python -m pytest tests/ -v` → **59/60 passed**（1 个预存失败：`test_settings_reads_seaweedfs_s3_config` monkeypatch 问题，与本次改动无关）
- 魔数校验：MP4/MOV 只认偏移 4 的 `ftyp`；WebP 双重校验 RIFF+WEBP；DOCX/PPTX 检查 PK 头
- SVG 清洗：含 `<script>` 的 SVG 被清洗干净，空结果被拒绝
- 前端搜索框 + rejected 筛选可以正常过滤作品列表

### 遗留问题

- 无需特殊处理

---

## 第 14 轮：测试体系补齐 + 代码质量提升（P4 + P6）

### 修改时间

2026-05-30

### 修改目标

补齐 S3 适配器、作品审核、点赞、完整流程、批量下载五大关键路径的自动化测试；消除 `_format_project` 重复代码、计算 SHA-256 文件指纹、为 `ProjectLike` 添加数据库唯一约束、点赞改为原子化 UPDATE。

依据：`docs/superpowers/specs/2026-05-26-c-group-next-steps-design.md` 大计划 P4 + P6。

### 修改内容

**P4 — 测试体系补齐**

1. **S3 适配器单元测试（moto mock）**
   - 新建：`tests/test_storage_s3.py`（7 个测试）
   - 覆盖：`save_bytes` → `open_stream` 读写闭环、私有桶、`exists`、`delete`、`open_stream` 404 → `FileNotFoundError`、`force_path_style` 配置传递
   - 使用 `moto[s3]` mock，不依赖真实 S3 服务

2. **审核接口集成测试**
   - 文件：`tests/test_integration_bugfixes.py`（4 个测试）
   - 覆盖：教师通过/驳回审核（含驳回理由）、非教师审核拒绝（403）、审核不存在作品返回 404

3. **点赞接口集成测试**
   - 文件：`tests/test_integration_bugfixes.py`（4 个测试）
   - 覆盖：点赞后 liked=true + likes=1、取消点赞 liked=false + likes=0、重复点赞不累积、点赞不存在作品返回 404

4. **作品完整流程 E2E 测试**
   - 文件：`tests/test_integration_bugfixes.py`（2 个测试）
   - 覆盖：创建 → pending 不在广场 → 审核通过 → 广场可见；驳回 → 重新提交 → 状态变回 pending

5. **批量下载测试**
   - 文件：`tests/test_integration_bugfixes.py`（3 个测试）
   - 覆盖：无可下载作品返回 404、本地存储下载返回 ZIP、通过 `report_file_id`（新方式）下载

**P6 — 代码质量与工程化**

6. **统一 `format_project`，消除重复代码**
   - 文件：`project_service.py` + `project_routes.py` + `teacher_routes.py`
   - `project_service.format_project()` 作为唯一规范版本，包含 `report_file_id`/`cover_file_id`
   - 删除 `project_routes.py` 和 `teacher_routes.py` 各自重复的 `_format_project` 函数
   - 清理无用 import（`User` 模型不再在路由层直接查询）

7. **上传时计算 SHA-256**
   - 文件：`upload_routes.py`
   - 上传成功后对内容做 `hashlib.sha256(content).hexdigest()`
   - 传入 `create_stored_file_record(sha256=...)` 存入 `stored_files.sha256`

8. **ProjectLike 添加唯一约束**
   - 文件：`entities.py`
   - `ProjectLike.__table_args__` 新增 `UniqueConstraint("user_id", "project_id", name="uq_project_like")`
   - 数据库层面兜底防止重复点赞

9. **点赞改为原子 UPDATE**
   - 文件：`project_service.py` `toggle_like`
   - 点赞数递增/递减改为 `UPDATE projects SET likes = likes +/- 1 WHERE id = :id`
   - 避免 SELECT + UPDATE 竞态条件导致点赞数不准确

10. **教师课程隔离**（上游 merge 已实现）
   - `teacher_service.py` 的 `list_all_projects` 通过 `_teacher_student_ids` 过滤教师课程下班级学生的作品
   - 教师只能审核/下载自己学生的作品

### 验证情况

- `python -m pytest tests/ -v` → **59/60 passed**（预存 1 个失败同上）
- S3 moto mock 7 个测试全部通过
- 审核、点赞、流程、批量下载 13 个新增集成测试全部通过
- `format_project` 统一后，作品广场、我的作品、作品详情、审核列表等路由行为一致
- `toggle_like` 原子 UPDATE 后并发安全性提升

### 遗留问题

- 无

---

## 第 15 轮：学生登录空白页修复 + 忘记密码安全改造（密保验证）

### 修改时间

2026-06-03

### 修改目标

1. 修复学生登录后首页内容空白、"需要老师权限"错误弹窗的线上 bug
2. 彻底改造忘记密码流程：从「只需学号即可重置」升级为「密保问题验证 + 人工审批」双通道方案，堵住身份冒用安全漏洞

### 修改内容

**一、学生登录 bug 修复**

1. **公告未读数接口路由注册错误修复**
   - 文件：`backend/app/api/v1/routes/announcement_routes.py`
   - 问题：`@router.get("/unread-count")` 装饰器缺少函数体，Python 将其与下方的 `/task-overview` 装饰器双叠包裹了 `overview()`（teacher 权限），导致学生调用时返回 403 "需要teacher权限"
   - 修复：将学生端函数 `get_unread_count` 移至 `/unread-count` 装饰器正下方

2. **学生登录首页空白修复**
   - 文件：`frontend/src/App.vue`
   - 问题：`<Transition mode="out-in">` 与 `router.replace('/')` 组合使用时，旧组件离开后新组件进入前出现空白间隙。只有学生端（跳转 `/`）受影响，教师/管理员端（跳转 `/teacher`/`/admin`）因跳过 Transition 不受影响
   - 修复：移除 `mode="out-in"`，改为默认模式（新旧同时过渡）

**二、忘记密码安全改造**

3. **数据库新增两张表**
   - 文件：`backend/app/models/entities.py`、`backend/app/db/schema_compat.py`
   - `security_questions`（id、user_id、question、answer_hash、created_at）：存储用户密保问题，答案使用 pbkdf2_sha256 哈希
   - `password_reset_requests`（id、user_id、message、status、resolved_by、new_password_hash、temp_password、resolved_at、created_at）：存储人工密码重置申请和审批记录

4. **新增 Pydantic Schema**
   - 文件：`backend/app/schemas/common.py`
   - 新增 8 个 Schema：`SecurityQuestionItem`、`SecurityQuestionIn`、`SecurityQuestionsUpdate`、`SecurityQuestionOut`、`ForgotPasswordCheckRequest`、`ForgotPasswordManualRequest`、`ResetRequestResolve`、`ResetRequestOut`
   - 旧 `ForgotPasswordRequest` 保留兼容

5. **密保问题管理服务**
   - 文件：`backend/app/services/auth_service.py`
   - `get_security_questions()` / `update_security_questions()`：CRUD，最多 3 个密保问题，整体替换
   - `get_forgot_password_questions()`：忘密页第一步，只返回问题文本
   - `verify_answers_and_reset_password()`：验证全部答案后重置密码
   - `submit_reset_request()`：提交人工重置申请（含留言，已有 pending 时拒绝重复）
   - `get_reset_requests_for_teacher()` / `get_reset_requests_for_admin()`：按角色和 status 筛选请求列表
   - `approve_reset_request()`：生成 8 位随机临时密码 → 重置 → `needs_password_change=true` → 存储 temp_password
   - `reject_reset_request()`：驳回申请

6. **密保验证暴力破解防护**
   - 文件：`backend/app/services/auth_service.py`
   - 新增 `_FORGOT_FAILURES` 内存计数器：同一 user_id 5 分钟内最多 5 次错误尝试
   - 超限返回 429 → 前端弹窗引导一键转人工申请
   - 验证通过后清除失败计数

7. **新增 API 端点**
   - 文件：`backend/app/api/v1/routes/auth_routes.py`
   - `GET /api/security-questions`（获取密保问题）
   - `PUT /api/security-questions`（设置密保问题）
   - `GET /api/password/forgot/questions?user_id=xxx`（忘密第一步）
   - `POST /api/password/forgot/reset`（答对后重置）
   - `POST /api/password/forgot/request`（提交人工申请）
   - 文件：`backend/app/api/v1/routes/teacher_routes.py`
   - `GET /api/teacher/password-reset-requests?status=xxx`（教师查看本班申请）
   - `POST /api/teacher/password-reset-requests/{id}/approve`（教师审批）
   - `POST /api/teacher/password-reset-requests/{id}/reject`（教师驳回）
   - 文件：`backend/app/api/v1/routes/admin_routes.py`
   - `GET /api/admin/password-reset-requests?status=xxx`（管理员查看全部）
   - `POST /api/admin/password-reset-requests/{id}/approve`（管理员审批）
   - `POST /api/admin/password-reset-requests/{id}/reject`（管理员驳回）

8. **前端忘记密码弹窗重构**
   - 文件：`frontend/src/views/LoginView.vue`
   - 从单页表单改为三步流程：输入学号 → 有密保则回答密保问题 / 无密保则填留言申请人工重置
   - 超限后弹窗引导一键切换到人工申请
   - 文件：`frontend/src/api/auth.ts` → 新增 `getForgotPasswordQuestions`、`resetPasswordWithAnswers`、`submitPasswordResetRequest`

9. **首次登录改密后引导设置密保**
   - 文件：`frontend/src/views/ChangePasswordView.vue`
   - 修改密码成功后弹出密保设置引导（可跳过，支持 1~3 个自定义问题）

10. **个人中心新增密保问题管理**
    - 文件：`frontend/src/views/ProfileView.vue`
    - 新增"密保问题" Tab：查看已有问题（答案遮蔽）、编辑/新增/删除密保问题

11. **教师端密码重置管理**
    - 文件：`frontend/src/views/teacher/TeacherStudentAdmin.vue`
    - 新增独立卡片"密码重置管理"：状态筛选（待处理/已通过/已驳回）、临时密码显示 + 一键复制、处理人名称、操作时间
    - 文件：`frontend/src/api/teacher.ts` → 新增 `PasswordResetRequest` 类型、`getPasswordResetRequests`、`approvePasswordResetRequest`、`rejectPasswordResetRequest`

12. **管理员端密码重置管理**
    - 新建：`frontend/src/views/admin/AdminPasswordReset.vue`
    - 管理员侧边栏新增"密码重置"入口
    - 文件：`frontend/src/api/admin.ts` → 新增 `getAdminPasswordResetRequests`、`adminApprovePasswordResetRequest`、`adminRejectPasswordResetRequest`
    - 文件：`frontend/src/router/index.ts` → 新增 `/admin/password-reset` 路由

13. **测试种子账号注释**
    - 文件：`backend/seed_data.py`
    - 将 5 个测试种子账号（admin/T001/2025001~2025003）注释，避免生产环境误用

### 验证情况

- `python -m pytest tests/ -v` → **65/66 passed**（1 个预存 Settings monkeypatch 失败，与本轮无关）
- 后端所有新模块导入无误
- 前端 `vue-tsc --noEmit` 类型检查通过
- 密保验证次数限制实测：第 1-4 次错误返回剩余次数，第 5 次返回 429 锁定，第 6 次仍锁定
- 自助重置、人工申请、教师审批、管理员审批流程完整走通

### 遗留问题

- 无

---

## 第 16 轮：课程列表修复 + 多选题支持 + 班级管理增强 + 成绩去重 + 时区修复

### 修改时间

2026-06-03 ~ 2026-06-04

### 修改目标

修复课程列表为空的线上 bug，新增多选题题型，增强班级编辑和学生导入校验，修复学生成绩跨班重复统计和公告时间 8 小时偏移问题。

外部贡献者：mahirushiina05（PR #13）

### 修改内容

1. **课程列表 API 统一返回格式（`658d3ff`）**
   - 文件：`backend/app/api/v1/routes/course_routes.py`、`backend/app/services/course_response_service.py`
   - 问题：教师端 `build_course_list` 返回 `{courses, hint}` 对象，前端期望数组，导致课程列表为空
   - 修复：`build_course_list` 统一返回平铺数组 `Course[]`，补全 `is_owner`/`is_public` 字段
   - 学生端和教师端使用同一返回格式

2. **多选题（multi_choice）支持（`658d3ff`）**
   - 文件：`backend/app/services/quiz_service.py`、`frontend/src/views/PracticeQuizView.vue`、`frontend/src/views/teacher/TeacherQuestions.vue`
   - 后端 `submit_answer` 新增 `multi_choice` 判分逻辑：排序后比对（`sorted(user_answer) == sorted(answer)`）
   - 前端练习页根据题型动态渲染复选框（multi_choice）或单选按钮（choice）
   - 题库管理新增 `multi_choice` 题型选项

3. **管理端公共课程资料上传改造（`658d3ff`）**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 资料弹窗改为文件上传组件，大小自动计算

4. **班级编辑接口（`6e527ef`）**
   - 文件：`backend/app/services/class_service.py`、`backend/app/api/v1/routes/class_routes.py`
   - 新增 `update_class()` 函数，支持修改班级名称和所属课程
   - 校验同名班级冲突，教师只能编辑自己创建的班级
   - 前端 `TeacherClasses.vue` 新增编辑弹窗

5. **手动添加学生校验增强（`6e527ef`）**
   - 文件：`backend/app/services/class_service.py`、`frontend/src/views/teacher/TeacherStudentAdmin.vue`
   - 新增学号姓名格式校验：已有学生时，输入姓名与系统记录不一致则拒绝
   - 新增 `major`（专业）字段支持
   - 前端表单新增专业输入框

6. **学生成绩跨班去重（`6e527ef`）**
   - 文件：`backend/app/services/teacher_service.py`
   - 问题：同一学生在多个班级时，学生成绩页会重复统计
   - 修复：`list_students` 按学生 ID 去重，多班级合并为一行展示（班级名用顿号分隔）
   - 分页在去重后执行，避免总数不准确

7. **任务概览统计口径修复（`6e527ef`）**
   - 文件：`backend/app/services/task_service.py`
   - 问题：`task_overview` 中 `total_completed`/`total_incomplete` 按任务累加，而非按学生去重
   - 修复：改为按学生维度汇总，学生完成所有分配任务 = 已完成，否则 = 未完成

8. **公告时间存储转 UTC（`6e527ef`）**
   - 文件：`backend/app/services/announcement_service.py`
   - 问题：前端传来北京时间 naive 字符串，直接存入数据库，学生端读取时再偏移 8 小时，导致时间不准确
   - 修复：`create_announcement` 在写入前将北京时间转为 UTC 存储，学生端统一用 `to_beijing_iso` 展示

### 验证情况

- 前端 `npm run build` 通过
- 后端相关测试通过

### 遗留问题

- 无

---

## 第 17 轮：教师端/管理员端/学生端全面优化

### 修改时间

2026-06-04

### 修改目标

全面优化教师端分页搜索、管理员端公共课程管理、学生端答题进度保存和课程搜索。新增跨端通用组件（图片占位、PDF 预览、上传进度）。

### 修改内容

1. **学生端答题进度保存**
   - 新建：`frontend/src/composables/useQuizDraft.ts`
   - 答题中途刷新或离开后，已答题目进度通过 localStorage 恢复
   - `PracticeQuizView.vue` 集成草稿保存和恢复逻辑

2. **学生端课程/资料搜索**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/CourseDetailView.vue`
   - 课程列表新增搜索框
   - 课程详情页资料支持搜索

3. **教师端全面分页和搜索**
   - 文件：`frontend/src/views/teacher/TeacherQuestions.vue`、`TeacherMaterials.vue`、`TeacherTaskReport.vue`、`TeacherCourses.vue`
   - 题库管理、资料管理、任务完成报告、课程管理全部新增分页和搜索
   - 后端 `material_service.list_materials` 新增 `keyword`/`page`/`page_size` 参数

4. **教师端资料管理增强**
   - 文件：`backend/app/services/material_service.py`、`backend/app/api/v1/routes/material_routes.py`
   - 新增 `can_view_course_materials()` 学生课程资料权限校验
   - 资料列表支持分页和关键词搜索

5. **学生端答题权限校验**
   - 文件：`backend/app/services/quiz_service.py`
   - `submit_answer` 新增 `role` 参数，学生只能答自己所属课程的题目
   - `get_quiz_stats` 四个统计指标统一限定在学生所属课程范围

6. **管理员端公共课程增强**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`、`backend/app/services/admin_public_course_service.py`
   - 公共课程同步状态展示
   - 资料在线预览

7. **跨端通用组件**
   - 新建：`frontend/src/components/common/AppImage.vue`（图片加载失败占位）
   - 新建：`frontend/src/components/common/PdfPreviewDialog.vue`（PDF 预览弹窗）
   - 新建：`frontend/src/composables/useUploadWithProgress.ts`（上传进度 composable）

8. **其他改进**
   - `TeacherAnnouncements.vue`：发布题目选题交互优化
   - `TeacherStudentAdmin.vue`：学生管理改进
   - `quiz_service.py`：答题后不再写入 StudentProgress（改为实时查询 QuizAttempt）

### 验证情况

- `cd frontend && npm run build` 通过
- 38 个文件变更，+1017/-195 行

### 遗留问题

- 无

---

## 第 18 轮：管理员端增强 + StudentProgress 删除 + 发布题目改造 + 安全增强

### 修改时间

2026-06-05

### 修改目标

删除已废弃的 StudentProgress 模型，管理员端新增公共课程 Excel 批量导入题目和删除教师级联清理，教师端发布题目改为只限自有课程，新增多角色授权函数，删除旧版无认证密码重置接口，清理已完成的设计文档。

### 修改内容

1. **删除 StudentProgress 模型**
   - 文件：`backend/app/models/entities.py`
   - `student_progress` 表和 ORM 模型完全移除
   - `User`/`Course` 模型的 `progress` relationship 删除
   - `quiz_service.py`：答题后不再写入 StudentProgress，`get_course_quiz_stats` 改为实时查询 QuizAttempt
   - `portfolio_service.py`：雷达图「理论基础」从 `learn_progress` 改为答题总量，`study_hours` 改为答题估算
   - `teacher_service.py`：学生列表不再显示 progress/exercises/accuracy
   - `class_service.py`、`question_service.py`：清理学生数据和课程删除检查中移除 StudentProgress

2. **管理员端公共课程 Excel 批量导入题目**
   - 文件：`backend/app/services/admin_public_course_service.py`、`frontend/src/views/admin/AdminPublicCourses.vue`、`frontend/src/api/adminPublicCourse.ts`
   - 管理员可为公共课程批量导入题目（复用 Excel 导入模板）

3. **删除教师级联清理**
   - 文件：`backend/app/api/v1/routes/admin_routes.py`、`frontend/src/views/admin/AdminTeachers.vue`
   - 删除教师时级联清理其所有关联数据（课程、班级、资料、题目、公告等）
   - 新增依赖检查 API，删除前展示关联数据统计

4. **发布题目改为只限自有课程**
   - 文件：`backend/app/services/announcement_service.py`、`frontend/src/views/teacher/TeacherAnnouncements.vue`
   - 后端课程查询从 `or_(created_by == teacher_id, is_public == True)` 收紧为 `created_by == teacher_id`
   - 前端课程下拉框只显示 `is_owner === true` 的课程
   - 新建：`frontend/src/views/teacher/questionSelection.ts`（选题状态同步工具）

5. **多角色授权函数**
   - 文件：`backend/app/core/security.py`
   - 新增 `require_roles(*roles)` 函数，支持同时接受多种角色

6. **删除旧版无认证密码重置接口**
   - 文件：`backend/app/api/v1/routes/auth_routes.py`
   - 移除旧版 `POST /api/password/forgot`（只需学号即可重置密码）

7. **安全修复**
   - 模板下载接口权限扩展到管理员
   - 学生课程答题统计权限校验补全

8. **文档清理**
   - 删除 7 个已完成的设计文档（教师数据隔离、C 组下一步、教师重构概述、课程/班级/发布/审核设计）
   - 新建：`docs/superpowers/plans/2026-06-05-publish-assignment-owned-courses.md`

9. **前端优化**
   - 首页轮播悬停暂停/恢复
   - `IntersectionObserver` 触发后 `unobserve`
   - CSS 颜色注释更新（造→思、练→践、行→悟）

### 验证情况

- 前端 `npm run build` 通过
- 31 个文件变更，+545/-1450 行（大量删除为清理设计文档）

### 遗留问题

- 无

---

## 第 19 轮：学生端学/思页面重构 — 作业精准练习与自由练习分离

### 修改时间

2026-06-06

### 修改目标

重构学生端"思"（练习）页面，将作业练习和自由练习分离为独立入口。作业练习支持按公告题目集精准练习和自动完成标记，自由练习保留按课程练习模式。新增随机练习功能和过期任务标注。

外部贡献者：Lemon（PR #15）

### 修改内容

1. **学/思页面功能分离（`735b68d`）**
   - 文件：`frontend/src/views/LearnView.vue`、`frontend/src/views/PracticeView.vue`
   - "学"页面（LearnView）新增作业任务入口，展示教师发布的公告任务列表
   - "思"页面（PracticeView）重构为自由练习入口，保留按课程练习模式
   - 后端 `course_response_service.py` 为学生端返回关联的公告任务数据

2. **作业精准练习（`735b68d`）**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增按公告题目集练习模式：只加载公告指定的 `question_ids`
   - 答题完成后自动标记任务完成（调用后端 `mark_completed`）

3. **随机练习功能（`01162fd`）**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增随机打乱题目顺序的练习模式

4. **作业/自由练习分离路由（`01162fd`）**
   - 新建：`frontend/src/views/PracticeAssignments.vue`（218 行）
   - 文件：`frontend/src/router/index.ts`
   - 新增 `/practice/assignments` 路由，独立的作业练习列表页
   - 表格化展示作业列表，标注过期/未开始状态

5. **后端题目接口调整**
   - 文件：`backend/app/api/v1/routes/question_routes.py`、`backend/app/services/announcement_service.py`
   - 题目接口支持按公告 ID 过滤（返回公告指定的题目子集）
   - 公告服务新增题目 ID 列表查询支持

### 验证情况

- 前端 `npm run build` 通过
- 9 个文件变更，+386/-192 行

### 遗留问题

- 无

---

## 第 20 轮：作品提交/审核消息通知系统

### 修改时间

2026-06-06

### 修改目标

新增学生通知系统：作品提交后教师端收到新作品提醒，教师审核（通过/驳回）后学生端收到审核结果通知。

外部贡献者：hanhan（PR #17）

### 修改内容

1. **新增 StudentNotification 模型**
   - 文件：`backend/app/models/entities.py`
   - 新增 `student_notifications` 表（id、user_id、type、title、content、project_id、is_read、created_at）
   - 关联 `User` 和 `Project` 模型

2. **通知服务**
   - 新建：`backend/app/services/notification_service.py`
   - `list_notifications()`：学生通知列表（按时间倒序）
   - `unread_count()`：未读通知计数
   - `mark_read()`：标记已读
   - `create_project_review_notification()`：作品审核结果推送（通过/驳回 + 原因）

3. **通知 API 路由**
   - 新建：`backend/app/api/v1/routes/notification_routes.py`
   - `GET /api/notifications`：学生通知列表
   - `GET /api/notifications/unread-count`：未读计数
   - `POST /api/notifications/{id}/read`：标记已读
   - 在 `__init__.py` 注册 `notification_router`

4. **作品审核触发通知**
   - 文件：`backend/app/services/project_service.py`
   - 教师审核通过或驳回时自动调用 `create_project_review_notification`

5. **前端通知展示**
   - 新建：`frontend/src/api/notification.ts`
   - 文件：`frontend/src/components/AppHeader.vue`、`frontend/src/components/AnnouncementPopup.vue`、`frontend/src/views/InboxView.vue`
   - 顶部导航新增未读通知角标
   - 收件箱页展示通知列表（区分公告任务和审核通知）
   - 教师端布局新增新作品提醒

6. **教师端新作品提示**
   - 文件：`frontend/src/views/teacher/TeacherLayout.vue`
   - 教师侧边栏或顶部新增待审核作品数量提示

### 验证情况

- 12 个文件变更，+361/-15 行

### 遗留问题

- `student_notifications` 表在 `schema_compat.py` 中的自动创建逻辑需确认

---

## 第 21 轮：前端样式和交互微调

### 修改时间

2026-06-06

### 修改目标

微调首页展示、练习答题交互和教师任务报告样式。

### 修改内容

1. **首页模块展示优化**
   - 文件：`frontend/src/components/home/ModuleShowcase.vue`、`frontend/src/views/HomeView.vue`
   - 模块展示卡片样式调整
   - 首页布局微调

2. **练习答题交互改进**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 答题交互细节优化（选项反馈、提交确认等）

3. **教师任务报告样式**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 表格列宽和布局微调

4. **全局样式**
   - 文件：`frontend/src/assets/main.css`
   - 颜色和间距微调

### 验证情况

- 5 个文件变更，+96/-35 行

### 遗留问题

- 无

---

## 第 22 轮：任务完成报告增强 + 公告题目任务闭环 + 消息中心体验优化

### 修改时间

2026-06-07

### 修改目标

1. 增强教师端任务完成报告，补充教师姓名、目标人数、按题去重计分等统计信息
2. 补齐公告题目任务练习闭环：学生端可通过任务精确跳转到对应题目集练习，答题记录关联任务维度
3. 修复消息中心（收件箱）体验问题：通知链接正确跳转、公告弹窗自动获取最新通知、顶部角标实时刷新

### 修改内容

**一、教师端任务完成报告增强**

1. **任务完成报告新增统计字段**
   - 文件：`backend/app/services/task_service.py`
   - `completion_report` 新增 `teacher_name`（教师姓名）、`target_count`（目标学生人数）、`completion_rate`（完成率）
   - `total_score` 计算改为按题去重（每题只计最新一次答题），解决重复答题超过 100 分的问题
   - `_is_expired` 提取为模块级函数，避免内部重复定义

2. **任务概览统计增强**
   - 文件：`backend/app/services/task_service.py`
   - `task_overview` 新增教师姓名和目标人数统计
   - 完成率计算基于目标人数

3. **前端成绩导出功能**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 新增 Excel 导出按钮，复用教师学生管理页导出能力

**二、公告题目任务练习闭环**

4. **新增「获取任务题目」后端接口**
   - 文件：`backend/app/api/v1/routes/announcement_routes.py`
   - 新增 `GET /api/announcements/{id}/questions`（学生端）
   - 校验学生班级归属、任务开始时间、截止时间
   - 返回公告信息 + 指定题目列表（含课程名、题型、选项、答案、解析）

5. **新增「获取任务题目」服务函数**
   - 文件：`backend/app/services/task_service.py`
   - 新增 `get_assignment_questions()` 函数
   - 集成任务权限校验（班级归属、开始/截止时间、题目过滤）

6. **答题记录关联任务维度**
   - 文件：`backend/app/models/entities.py`
   - `QuizAttempt` 新增 `announcement_id` 字段（可选，关联题目任务 ID）
   - 文件：`backend/app/schemas/common.py`
   - `SubmitAnswerRequest` 新增 `announcement_id` 可选字段
   - 文件：`backend/app/api/v1/routes/quiz_routes.py`
   - `submit_answer` 传递 `announcement_id` 参数
   - 文件：`backend/app/services/quiz_service.py`
   - `submit_answer` 写入 `announcement_id` 到 QuizAttempt

7. **数据库 Schema 兼容**
   - 文件：`backend/app/db/schema_compat.py`
   - `_ensure_column` 新增 `quiz_attempts.announcement_id` 列自动创建
   - 文件：`backend/tests/test_schema_compat.py`
   - 新增 `announcement_id` 兼容列测试

8. **前端任务练习页重构**
   - 文件：`frontend/src/views/PracticeQuizView.vue`
   - 新增 `announcementId` 路由参数支持
   - 有 `announcementId` 时调用 `/announcements/{id}/questions` 获取题目
   - 答题提交时携带 `announcement_id` 关联任务
   - 随机练习按钮文案改为"随机练习"

9. **前端任务题目 API**
   - 文件：`frontend/src/api/announcement.ts`
   - 新增 `getAssignmentQuestions()` 函数
   - 文件：`frontend/src/api/quiz.ts`
   - `SubmitAnswerRequest` 新增 `announcement_id` 可选字段

10. **前端新增任务练习路由**
    - 文件：`frontend/src/router/index.ts`
    - 新增 `/practice/announcement/:announcementId` 路由，组件复用 `PracticeQuizView`

**三、消息中心体验优化**

11. **收件箱任务链接修复**
    - 文件：`frontend/src/views/InboxView.vue`
    - 题目任务的"去练习"按钮链接改为 `/practice/announcement/:id`，跳转到对应任务题目集
    - 新增跳转函数，区分任务类型和作品通知的不同跳转目标

12. **公告弹窗自动获取最新通知**
    - 文件：`frontend/src/components/AnnouncementPopup.vue`
    - 弹窗显示时自动调用通知列表 API 获取最新一条通知（补充公告轮询遗漏的审核通知）

13. **顶部未读角标实时刷新**
    - 新建：`frontend/src/utils/messageRefresh.ts`
    - 提供 `emitMessageRefresh()` / `onMessageRefresh()` 全局事件机制
    - 文件：`frontend/src/components/AppHeader.vue`
    - 监听消息刷新事件，实时更新未读角标
    - 文件：`frontend/src/views/InboxView.vue`
    - 标记已读后触发 `emitMessageRefresh()` 同步头部角标
    - 文件：`frontend/src/components/AnnouncementPopup.vue`
    - 标记已读后触发 `emitMessageRefresh()` 同步头部角标

**四、项目地图更新**

14. **project-map.md 更新**
    - 文件：`docs/superpowers/project-map.md`
    - 新增 `Announcement → QuizAttempt(announcement_id) → TaskCompletion` 数据关系
    - 新增 `/practice/announcement/:announcementId` 路由
    - 新增题目任务练习的 `announcement_id` 关联约定

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/models/entities.py` | 修改（QuizAttempt 新增 announcement_id） |
| `backend/app/schemas/common.py` | 修改（SubmitAnswerRequest 新增 announcement_id） |
| `backend/app/services/task_service.py` | 修改（completion_report 增强 + get_assignment_questions） |
| `backend/app/services/quiz_service.py` | 修改（submit_answer 写入 announcement_id） |
| `backend/app/api/v1/routes/announcement_routes.py` | 修改（新增 /questions 端点） |
| `backend/app/api/v1/routes/quiz_routes.py` | 修改（传递 announcement_id） |
| `backend/app/db/schema_compat.py` | 修改（quiz_attempts.announcement_id 兼容列） |
| `backend/tests/test_schema_compat.py` | 修改（新增测试） |
| `backend/tests/test_integration_bugfixes.py` | 修改（新增集成测试） |
| `backend/tests/test_assignment_practice_flow.py` | 新建（任务练习流程测试） |
| `frontend/src/api/announcement.ts` | 修改（新增 getAssignmentQuestions） |
| `frontend/src/api/quiz.ts` | 修改（SubmitAnswerRequest 新增 announcement_id） |
| `frontend/src/views/PracticeQuizView.vue` | 修改（支持任务练习模式） |
| `frontend/src/views/InboxView.vue` | 修改（通知跳转修复 + 角标刷新） |
| `frontend/src/components/AnnouncementPopup.vue` | 修改（自动获取最新通知 + 角标刷新） |
| `frontend/src/components/AppHeader.vue` | 修改（监听消息刷新事件） |
| `frontend/src/router/index.ts` | 修改（新增任务练习路由） |
| `frontend/src/utils/messageRefresh.ts` | 新建（消息刷新事件工具） |
| `frontend/index.html` | 修改（标题文案微调） |
| `frontend/package.json` | 修改（项目名微调） |
| `frontend/README.md` | 修改（精简） |
| `backend/README.md` | 修改（测试命令说明） |
| `docs/superpowers/project-map.md` | 修改（数据关系和路由更新） |

### 验证情况

- 前端 `npm run type-check` 通过
- 后端 `py -m pytest tests/ -v` 通过
- 任务完成报告新增字段（教师姓名、目标人数、完成率）正确返回
- 按题去重计分后分数不会超过 100
- 学生端任务练习可跳转到对应题目集
- 答题记录正确携带 `announcement_id`
- 未读角标在标记已读后实时刷新

### 遗留问题

- 无

---

## 第 23 轮：课程详情学生权限收尾 + 项目状态同步

### 修改时间

2026-06-24

### 修改目标

收紧学生访问课程详情的权限边界，合并本轮修改记录到唯一总记录文件，并把项目状态从旧清单同步到当前代码事实。

### 修改内容

1. **课程详情学生权限校验**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 学生访问 `/api/courses/{course_id}` 时先校验是否已加入该课程，未加入时返回 `课程不存在`。
   - 文件：`backend/app/api/v1/routes/question_routes.py`
   - 兼容入口 `/api/questions/courses/{course_id}` 使用同样的学生课程访问校验。

2. **课程详情权限回归测试**
   - 文件：`backend/tests/test_student_course_visibility.py`
   - 新增学生不能访问未加入课程详情的回归测试，覆盖两个课程详情入口。

3. **教师端作业完成静态测试同步**
   - 文件：`frontend/src/views/teacher/TeacherTaskReport.vue`
   - 保留 `/teacher/task-report?task_id=...` 跳转到任务完成详情的入口行为。
   - 文件：`frontend/tests/teacher-task-report-migration-static.test.mjs`
   - 将函数名断言改为行为级断言，匹配详情跳转函数和详情路由名。

4. **项目状态同步**
   - 文件：`AGENTS.md`、`CLAUDE.md`
   - 标记教师端学生成绩作业分数展示已完成。
   - 标记课程详情学生权限和 `mark_completed` 校验已完成或已核实。
   - 明确后续修改记录统一追加到 `backend/docs/项目修改记录.md`，不再按日期新建分散记录文件。

5. **本地输出目录忽略**
   - 文件：`.gitignore`
   - 保留 `output/` 忽略规则，避免本地生成输出进入版本管理。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/course_routes.py` | 修改（课程详情学生权限校验） |
| `backend/app/api/v1/routes/question_routes.py` | 修改（兼容课程详情入口权限校验） |
| `backend/tests/test_student_course_visibility.py` | 修改（新增回归测试） |
| `frontend/src/views/teacher/TeacherTaskReport.vue` | 修改（恢复 task_id 查询参数跳转详情） |
| `frontend/tests/teacher-task-report-migration-static.test.mjs` | 修改（静态断言改为行为级） |
| `.gitignore` | 修改（忽略 output/ 本地输出目录） |
| `AGENTS.md` | 修改（同步当前优先级和记录规范） |
| `CLAUDE.md` | 修改（同步修改记录入口说明） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录和单文件记录规范） |

### 验证情况

- `backend\.venv\Scripts\pytest.exe backend\tests\test_auth.py backend\tests\test_student_course_visibility.py backend\tests\test_assignment_practice_flow.py -q` 通过：31 passed, 1 warning
- `PYTEST_ADDOPTS="--basetemp C:\Users\ASUS\AppData\Local\Temp\pytest-codex-tongshi" backend\.venv\Scripts\pytest.exe backend\tests -q` 通过：139 passed, 1 warning
- `node frontend/tests/teacher-task-report-migration-static.test.mjs` 通过
- `cmd /c npm run build` 通过，仍有 Vite chunk size warning

### 遗留问题

- `fastapi.testclient` 仍有 `StarletteDeprecationWarning`，不影响本轮修复。

---

## 第 24 轮：服务器部署影响说明规则补充

### 修改时间

2026-06-24

### 修改目标

补充长期协作规则：项目已经部署到服务器，后续每次修改完成后都必须明确说明服务器侧需要同步执行哪些操作。

### 修改内容

1. **项目规则补充服务器部署影响说明**
   - 文件：`AGENTS.md`
   - 在文档同步和验收底线中增加服务器部署影响说明要求。
   - 每次总结必须说明服务器是否需要拉代码、重启服务、重新构建前端、执行数据库迁移、调整环境变量或修改配置。
   - 即使不需要服务器操作，也必须明确写明“不需要服务器修改”。

2. **本地协作记忆同步**
   - 文件：`CLAUDE.md`
   - 修改项目修改记录说明，补充服务器部署影响说明要求。

3. **总修改记录模板更新**
   - 文件：`backend/docs/项目修改记录.md`
   - 维护建议模板新增“服务器部署影响”栏目，后续修改记录必须填写。

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `AGENTS.md` | 修改（新增服务器部署影响说明规则） |
| `CLAUDE.md` | 修改（同步服务器部署影响说明要求） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮记录并更新维护模板） |

### 验证情况

- `git diff --check` 通过

### 服务器部署影响

- 本轮只修改项目规则和修改记录文档，不需要服务器拉代码、重启服务、重新构建前端、执行数据库迁移或调整配置。

### 遗留问题

- 无

---

## 第 25 轮：课程阶段化改造联动缺陷修复

### 修改时间

2026-06-25

### 修改目标

修复课程阶段化改造后三端（管理员/教师/学生）联动中发现的 8 项功能缺陷，包括 API 模板字符串错误、不存在的属性引用、stage_id 无法清空、阶段接口权限缺失、事务控制不当等问题。

### 修改内容

1. **修复 adminPublicCourse.ts 16 处 URL 模板字符串缺失反引号和插值**
   - 文件：`frontend/src/api/adminPublicCourse.ts`
   - 问题：所有动态 URL 使用普通字符串而非模板字符串，导致 `${courseId}` 等变量未被解析，请求地址错误
   - 修复：全部改为反引号模板字符串，共修复 16 处

2. **修复 course_response_service.py 中 `material.is_synced` 不存在属性**
   - 文件：`backend/app/services/course_response_service.py`
   - 问题：`_format_material` 使用 `material.is_synced`，但 `Material` 模型没有该属性
   - 修复：改为 `bool(material.source_material_id)`

3. **修复 course_response_service.py 阶段数据补充 source_stage_id 和排序稳定性**
   - 文件：`backend/app/services/course_response_service.py`
   - 修复：`build_course_detail` 中 stage_data 补充 `source_stage_id` 字段；排序键改为 `(sort_order, id)` 保证稳定性

4. **修复 stage_id 无法通过 API 显式清空为 null**
   - 文件：`backend/app/services/material_service.py`、`backend/app/api/v1/routes/material_routes.py`
   - 问题：Pydantic `Optional[int] = None` 无法区分"未传"和"显式 null"
   - 修复：`update_material` 新增 `clear_stage_id` 参数；路由层通过 `model_fields_set` 判断是否显式传 null

5. **修复管理员删除公共课程时未清理 source_stage_id**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：`delete_public_course` 清理了同步资料和题目，但遗漏了 `source_stage_id` 引用
   - 修复：新增清理查询，将副本阶段的 `source_stage_id` 置 null

6. **修复课程阶段接口缺少学生访问权限校验**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 问题：`get_course_stages` 对学生无 enrolled 检查，学生可直接访问未加入课程的阶段
   - 修复：增加 `student_can_access_course` 校验

7. **修复 course_stage_service 中过早 commit 导致路由层无法控制事务**
   - 文件：`backend/app/services/course_stage_service.py`、`backend/app/api/v1/routes/stage_routes.py`
   - 问题：服务层直接 `db.commit()`，路由层无法追加后续操作或统一回滚
   - 修复：服务层改为 `db.flush()`，路由层负责 `db.commit()`

8. **修复管理员编辑阶段名称未 strip 空格**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`、`backend/app/api/v1/routes/stage_routes.py`
   - 修复：对 `data.name` 添加 `.strip()` 处理

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `frontend/src/api/adminPublicCourse.ts` | 修改（修复 16 处 URL 模板字符串） |
| `backend/app/services/course_response_service.py` | 修改（is_synced、source_stage_id、排序） |
| `backend/app/services/material_service.py` | 修改（clear_stage_id 参数） |
| `backend/app/api/v1/routes/material_routes.py` | 修改（model_fields_set 判断） |
| `backend/app/services/admin_public_course_service.py` | 修改（source_stage_id 清理） |
| `backend/app/api/v1/routes/course_routes.py` | 修改（学生访问校验） |
| `backend/app/services/course_stage_service.py` | 修改（commit 改 flush） |
| `backend/app/api/v1/routes/stage_routes.py` | 修改（commit + strip） |
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（strip） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（涉及路由和服务层改动）
- 需要重新构建前端（adminPublicCourse.ts 修复）
- 不需要执行数据库迁移（无表结构变更，schema_compat 自动迁移已在第 24 轮就绪）
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 26 轮：课程阶段化联动深度审查修复

### 修改时间

2026-06-25

### 修改目标

对第 25 轮修复后的代码进行第二轮深度审查，修复新发现的 6 项高优先级问题，包括管理员阶段操作权限越界、公共资料 stage_id 无校验、表单嵌套错误、前端错误处理缺失、题库管理 Tab 缺失和课程详情页滚动监听缺失。

### 修改内容

1. **修复管理员操作阶段时 teacher_id 权限越界**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：管理员创建/删除阶段时传入 `current_user.id` 作为 `teacher_id`，导致管理员无法操作非自己创建的阶段
   - 修复：管理员路由中移除 teacher_id 参数

2. **修复管理员新增/修改公共资料时 stage_id 无归属校验**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：创建或修改公共资料时未校验 stage_id 是否属于目标课程，可传入任意课程阶段
   - 修复：新增 `_validate_stage_id` 辅助函数，在 `create_public_material` 和 `update_public_material` 中调用

3. **修复管理员资料修改时 stage_id 无法设为 null + update 使用 exclude_unset**
   - 文件：`backend/app/services/admin_public_course_service.py`、`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：update 循环跳过所有 None 值，无法显式清空 stage_id；且未使用 `exclude_unset=True`
   - 修复：循环允许 stage_id 为 None 时赋值；路由改为 `model_dump(exclude_unset=True)`

4. **修复 AdminPublicCourses.vue 资料表单 el-form-item 嵌套错误**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 问题："资料标题" 的 `el-form-item` 未正确关闭，导致"所属阶段"被嵌套在内部
   - 修复：重构为正确的并列结构

5. **修复 AdminPublicCourses.vue 题库管理 Tab 缺失**
   - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
   - 问题：题目 CRUD 函数和弹窗代码已存在，但 `<el-tabs>` 中缺少"题库管理"`<el-tab-pane>`，UI 无法访问
   - 修复：新增"题库管理"Tab，包含新增/批量导入工具栏和题目列表表格

6. **修复 CourseDetailView.vue 侧边栏滚动高亮不跟随**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：用户手动滚动时，左侧目录的 active 状态不会更新
   - 修复：添加 IntersectionObserver 监听所有 `.stage-section`，进入视口时自动更新 `activeStageId`；`onUnmounted` 清理观察器

7. **补充 CourseDetailView.vue 错误处理**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：`loadCourse` 的 try/catch 缺少错误提示
   - 修复：catch 中添加 `ElMessage.error` 提示用户网络异常

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（teacher_id 移除、exclude_unset） |
| `backend/app/services/admin_public_course_service.py` | 修改（_validate_stage_id、stage_id null 处理） |
| `frontend/src/views/admin/AdminPublicCourses.vue` | 修改（表单嵌套修复、题库管理 Tab） |
| `frontend/src/views/CourseDetailView.vue` | 修改（IntersectionObserver、错误处理） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试全部通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（涉及路由和服务层改动）
- 需要重新构建前端（AdminPublicCourses.vue 和 CourseDetailView.vue 改动）
- 不需要执行数据库迁移（无表结构变更）
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 27 轮：三端联动深度审查与数据完整性加固

### 修改时间

2026-06-25

### 修改目标

对三端联动进行第三轮深度代码审查，修复 15 项高/中/低优先级问题，涵盖数据一致性、事务安全、前端交互体验和数据库迁移完整性。

### 修改内容

1. **修复编辑公共题目时 model_dump 缺少 exclude_unset（高）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`edit_public_question` 使用 `data.model_dump()` 输出全部字段（含默认值），导致部分更新时未传字段被默认值覆盖（如 options=[]、tags=[]），且同步传播到教师副本
   - 修复：改为 `data.model_dump(exclude_unset=True)`

2. **修复批量导入题目 flush 失败后 Session 损坏（高）**
   - 文件：`backend/app/services/admin_public_course_service.py`
   - 问题：`import_questions_to_public_course` 循环中 `db.flush()` 失败后 Session 进入无效状态，后续行全部失败，已成功行也被回滚
   - 修复：每行使用 `db.begin_nested()` 保存点隔离，失败时仅回滚该行

3. **修复教师端创建阶段缺少 db.commit()（高）**
   - 文件：`backend/app/api/v1/routes/course_routes.py`
   - 问题：`add_course_stage` 调用 `create_stage`（内部仅 flush），路由层未 commit，请求结束后 Session 关闭导致隐式回滚，新建阶段丢失
   - 修复：`create_stage()` 后补 `db.commit()` + `db.refresh(stage)`

4. **修复管理员编辑阶段时 course_id 归属校验在 flush 之后（高）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`edit_public_stage` 先调用 `update_stage`（内部 flush 已写入修改），后才检查 `stage.course_id != course_id`，导致数据不一致
   - 修复：调用 `update_stage` 之前先查询并验证阶段归属

5. **修复删除公共阶段后未清理教师副本中的对应阶段（中）**
   - 文件：`backend/app/api/v1/routes/admin_public_course_routes.py`
   - 问题：`remove_public_stage` 删除源阶段后调用 `sync_stages_to_course_copies`，但该函数只做创建+更新不做删除，副本中残留孤儿阶段
   - 修复：删除源阶段前，先查找并删除所有 `source_stage_id` 引用该阶段的副本阶段（副本阶段下有资料时将其设为未分类）

6. **schema_compat 补建 showcase_item_images 表（中）**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：ORM 模型 `ShowcaseItemImage` 存在但迁移脚本未创建该表，旧库升级后访问图文内容图片时报 500
   - 修复：在 `showcase_items` 表创建后增加 `showcase_item_images` 表创建逻辑（含 FK 和索引）

7. **schema_compat 补建 source_stage_id FK 约束和索引（中）**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：迁移创建的 `course_stages` 表缺少 `source_stage_id` 外键约束和索引，同步服务依赖此字段查询
   - 修复：添加 `ix_course_stages_source_stage_id` 索引和 `fk_course_stages_source_stage_id` 外键（ON DELETE SET NULL）

8. **学生端课程详情资料链接空值防护（中）**
   - 文件：`frontend/src/views/CourseDetailView.vue`
   - 问题：资料 `file_id` 和 `url` 均为空时，`<a href="">` 导致页面自跳转
   - 修复：`<a>` 改为 `<div @click="openMaterial(...)">`，`openMaterial` 函数检查空 URL 并弹出提示

9. **教师端上传资料错误提示区分（中）**
   - 文件：`frontend/src/views/teacher/TeacherCourseDetail.vue`
   - 问题：文件上传成功后 `createMaterial` 失败时，统一显示"上传失败"，误导用户重新上传
   - 修复：分拆 try/catch，分别提示"文件上传失败"和"资料记录创建失败，文件已上传"

10. **学生端侧边栏添加未分类资料导航入口（中）**
    - 文件：`frontend/src/views/CourseDetailView.vue`
    - 问题：侧边栏仅显示阶段列表，有未分类资料时无导航入口
    - 修复：在阶段列表末尾添加"未分类资料"toc-item，点击滚动至对应区域

11. **Material.stage_id FK 添加 ondelete="SET NULL"（低）**
    - 文件：`backend/app/models/entities.py`
    - 修复：阶段删除时资料自动回到未分类状态，避免 FK 约束阻止删除

12. **update 循环排除敏感字段（低）**
    - 文件：`backend/app/services/admin_public_course_service.py`
    - 修复：`update_public_question` 和 `update_public_material` 的循环中增加 `_PROTECTED_FIELDS` 集合，排除 id、course_id、source_*_id、created_at

13. **移除 AdminPublicCourses.vue 中未使用的 selectedCourseId（低）**
    - 文件：`frontend/src/views/admin/AdminPublicCourses.vue`
    - 修复：删除死代码

14. **公共课程 description 字段添加 strip 处理（低）**
    - 文件：`backend/app/services/admin_public_course_service.py`
    - 修复：`create_public_course` 和 `update_public_course` 中对 description 调用 `.strip()`

15. **IntersectionObserver forEach 非空断言改进（低）**
    - 文件：`frontend/src/views/CourseDetailView.vue`
    - 修复：`observer.value!.observe()` 改为先捕获局部变量再遍历，避免极端情况下的空引用

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/api/v1/routes/admin_public_course_routes.py` | 修改（exclude_unset、归属校验、阶段清理、Material 导入） |
| `backend/app/api/v1/routes/course_routes.py` | 修改（add_course_stage 补 commit） |
| `backend/app/services/admin_public_course_service.py` | 修改（begin_nested、_PROTECTED_FIELDS、strip） |
| `backend/app/models/entities.py` | 修改（Material.stage_id ondelete） |
| `backend/app/db/schema_compat.py` | 修改（showcase_item_images 表、source_stage_id FK/索引） |
| `frontend/src/views/CourseDetailView.vue` | 修改（openMaterial、侧边栏、IntersectionObserver） |
| `frontend/src/views/teacher/TeacherCourseDetail.vue` | 修改（上传/创建错误提示分离） |
| `frontend/src/views/admin/AdminPublicCourses.vue` | 修改（移除死代码） |

### 验证情况

- `npm run build` 通过（含 vue-tsc type-check），构建产物正常输出到 dist/
- 后端 143 项测试全部通过

### 服务器部署影响

- 需要拉取代码并重启后端服务（路由、服务、模型层均有改动）
- 需要重新构建前端（3 个 Vue 组件改动）
- schema_compat 会自动补建 showcase_item_images 表和 source_stage_id 索引/FK，无需手动数据库迁移
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 第 28 轮：MySQL TEXT 默认值启动失败热修复

### 修改时间

2026-06-27

### 修改目标

修复服务器更新后后端启动失败的问题。线上 MySQL 在执行 `schema_compat.py` 自动补齐 `courses.description` 字段时，拒绝 `TEXT DEFAULT ''`，导致 `tongshi-backend` 反复退出。

### 修改内容

1. **修复 `courses.description` 兼容补列 SQL**
   - 文件：`backend/app/db/schema_compat.py`
   - 问题：MySQL 不允许 `TEXT` 类型字段设置默认值，原 SQL `ALTER TABLE courses ADD COLUMN description TEXT DEFAULT ''` 会触发 `(1101, "BLOB, TEXT, GEOMETRY or JSON column 'description' can't have a default value")`
   - 修复：改为 `ALTER TABLE courses ADD COLUMN description TEXT NULL`，保持字段可空，由业务层按需处理空值

2. **补充 MySQL 兼容回归测试**
   - 文件：`backend/tests/test_schema_compat.py`
   - 新增测试模拟 MySQL 旧库缺少 `courses.description` 时的兼容脚本输出，确保不会再次生成 `TEXT DEFAULT ''`

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `backend/app/db/schema_compat.py` | 修改（修复 MySQL TEXT 补列 SQL） |
| `backend/tests/test_schema_compat.py` | 修改（新增回归测试） |
| `backend/docs/项目修改记录.md` | 修改（追加本轮热修复记录） |

### 验证情况

- `python -m pytest backend\tests\test_schema_compat.py::test_ensure_schema_compatibility_adds_course_description_without_mysql_text_default -q` 通过
- `python -m pytest backend\tests\test_schema_compat.py -q` 通过：14 passed

### 服务器部署影响

- 需要拉取代码并重启后端服务，后端启动时会自动用 `TEXT NULL` 补齐 `courses.description`
- 不需要重新构建前端（无前端文件改动）
- 不需要手动数据库迁移；如果服务器尚未成功添加 `courses.description`，本次代码会在启动时自动补齐
- 不需要修改环境变量或 Nginx 配置

### 遗留问题

- 无

---

## 维护建议

`backend/docs/项目修改记录.md` 是项目唯一正式修改记录文件。以后每次代码、接口、页面、构建、部署或测试相关改动完成后，都必须把变更追加到本文件；不要再为单次改动新建分散的日期记录文件。

建议继续按下面格式追加：

```text
## 第 N 轮：标题

### 修改时间
...

### 修改目标
...

### 修改内容
...

### 验证情况
...

### 服务器部署影响
...

### 遗留问题
...
```

这样项目的演进历史会非常清晰。
